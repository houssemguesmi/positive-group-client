const Course = require("../models/course");

module.exports = {
  getAllCourses: async (req, res) => {
    try {
    } catch (e) {
      console.error(e);
    }
  },

  unlockCourse: async (req, res) => {
    try {
    } catch (e) {
      console.error(e);
    }
  },
};
